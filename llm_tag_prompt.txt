「解析対象」以下には法令がjson形式で記載されている。
この内容を以下のステップでタグの配列に変換し、指定のjsnoフォーマットで出力してほしい。
# step1: 「解析対象」を100文字以下の構造を平文として要約する。
# step2: step1の要約結果から、要約内容を特徴づける適切な単語を最大5つ選ぶ
# step3: 選んだ単語を以下のjson形式で回答する。 **必ず 以下のJSON 形式でのみ** 回答してください。

JSONフォーマット:
{
  "tags": ["tag1", "tag2", "tag3", "tag4", "tag5"]
}

「解析対象」
